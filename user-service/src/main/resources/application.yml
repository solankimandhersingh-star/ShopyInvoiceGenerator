server:
  port: 8083
  forward-headers-strategy: framework


spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/InvoiceGenerator_1
    username: root
    password: Mahi@1997
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect